version: '3.8'

services:
  springboot-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: springboot-container
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://dpg-d3oe46ripnbc73fu1ang-a.oregon-postgres.render.com:5432/retaildb_bs4x?sslmode=require
      SPRING_DATASOURCE_USERNAME: retaildb_bs4x_user
      SPRING_DATASOURCE_PASSWORD: Ve29LGlnTqHX2HI7GhY2hxalUcN9ESaJ
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SPRING_JPA_SHOW_SQL: "true"
      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
      SPRING_JPA_HIBERNATE_NAMING_PHYSICAL_STRATEGY: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      SPRING_JACKSON_PROPERTY_NAMING_STRATEGY: SNAKE_CASE
    networks:
      - springboot-net

networks:
  springboot-net:
    driver: bridge
